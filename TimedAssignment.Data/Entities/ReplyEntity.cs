using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace TimedAssignment.Data.Entities
{
    public class ReplyEntity
    {
        // Reply Class (either as a completely separate class, or have it inherit from Comment - your choice)
        // int Id
        // (Foreign Key to Comment via Id w/ virtual Comment)
        // string Text
        // Guid AuthorId
    }
}